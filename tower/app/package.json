{
  "name": "workkit-tower",
  "version": "1.0.0",
  "private": true,
  "description": "The tower's UI - an OMEGA brand root whose one app is the mission control dashboard. Its own npm root: npm workspaces do not nest, so this is never folded into workkit's root package.json.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start": "omega dev",
    "dev": "omega dev",
    "build": "omega build",
    "deploy": "omega deploy",
    "manage": "omega manage"
  },
  "devDependencies": {
    "@omega.js/manager": "file:../../../../Omega/omega/packages/manager"
  },
  "engines": {
    "node": ">=22"
  }
}
